my_tuple= (1, 2, 3, 4, 5)
# print(my_tuple[0] + my_tuple[1])
# print(my_tuple[-1] + my_tuple[-2])

# my_tuple[3] = 1  # cannot do this, tuples are immutable
# you can do less with tuples but they take up less memory

my_tuple = (5, 4, 3, 2, 1)
print(my_tuple)